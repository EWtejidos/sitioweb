<div class="public-page">
  {% render 'page-header' %}
  <main class="glass-surface collection-shell">
    <div class="section-header collection-header">
      <div>
        <p class="section-eyebrow">Pagina</p>
        <h1 class="section-title">{{ page.title }}</h1>
      </div>
    </div>
    <div class="hero-text rte">
      {{ page.content }}
    </div>
  </main>
</div>

{% schema %}
{
  "name": "Main page",
  "settings": []
}
{% endschema %}
